
  # Create mockup from markdown

  This is a code bundle for Create mockup from markdown. The original project is available at https://www.figma.com/design/sU80x4lyhcyjHtPeUFtzgq/Create-mockup-from-markdown.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  